#include<iostream>
#include<algorithm>

using namespace std;

int main(){
    long n,temp;
    cin>>n;
    vector<long> a;
    for(int i=0;i<n;i++){
        cin>>temp;
        a.push_back(temp);
    }
    map<long,int> M;
    for(int i=0;i<;i++)
        M[a[i]]=i;
    a.sort();

    return 0;
}
